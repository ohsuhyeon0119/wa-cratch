# #27~#30 QA 버그 수정 플랜

## 영향 범위

- 수정: `frontend/src/pages/LoginPage/LoginPage.tsx`
- 수정: `backend/app/services/project_service.py`
- 생성: `backend/app/models/like.py`
- 생성: `backend/app/models/follow.py`
- 수정: `backend/app/models/__init__.py`
- 생성: `backend/app/routers/likes.py`
- 생성: `backend/app/routers/follows.py`
- 수정: `backend/app/main.py`
- 수정: `backend/app/schemas/__init__.py`
- 수정: `frontend/src/api/projects.ts`
- 생성: `frontend/src/api/likes.ts`
- 생성: `frontend/src/api/follows.ts`
- 수정: `frontend/src/pages/PlayPage/PlayPage.tsx`
- 수정: `frontend/src/pages/DashboardPage/DashboardPage.tsx`

---

## 태스크

### Task 1 — [#27] 로그인·회원가입 에러 메시지 픽스 (3분)
- **파일**: `frontend/src/pages/LoginPage/LoginPage.tsx`
- **구현**:
  - `handleLogin` catch 블록: `axios.isAxiosError` 체크 후 401/422/기타 분기
  - `handleSignup` catch 블록: 동일하게 422/기타 분기
  - axios import 추가
- **검증**: 브라우저에서 빈 폼 로그인 시도 / 잘못된 계정 로그인 시도

### Task 2 — [#28] author 필드 nickname으로 변경 (3분)
- **파일**: `backend/app/services/project_service.py`
- **구현**:
  - `_to_dict` 파라미터 `author_username` → `author_nickname`
  - `list_public_projects`: `User.username` → `User.nickname` join select
  - `list_user_projects`: 동일
  - `get_project`: 동일
  - `update_project`: 동일
- **검증**: `/projects` API 응답의 `author` 필드가 닉네임인지 확인

### Task 3 — [#29] Like 모델 + 마이그레이션 (3분)
- **파일**: `backend/app/models/like.py` (신규)
- **구현**:
  - `Like` 모델: `user_id`, `project_id` (UniqueConstraint로 중복 방지)
  - `backend/app/models/__init__.py`에 Like import 추가
- **검증**: `docker-compose up db` 후 테이블 생성 확인

### Task 4 — [#29] 좋아요 API 구현 (4분)
- **파일**: `backend/app/routers/likes.py` (신규)
- **구현**:
  - `POST /projects/{project_id}/like` — 좋아요 토글 (있으면 취소, 없으면 추가), `Project.likes` 카운트 동기화
  - `GET /projects/liked` — 내가 좋아요한 프로젝트 목록 (JWT 필요)
  - `backend/app/main.py`에 라우터 등록
  - 응답 스키마: `LikeResponse { liked: bool, likes: int }`
- **검증**: `hurl --test backend/tests/hurl/` (기존 테스트 통과 확인)

### Task 5 — [#29] 프론트엔드 좋아요 API + PlayPage 카운트 동기화 (4분)
- **파일**: `frontend/src/api/likes.ts` (신규), `frontend/src/pages/PlayPage/PlayPage.tsx`
- **구현**:
  - `likeProject(id)` / `unlikeProject(id)` 함수
  - `PlayPage` 좋아요 클릭 핸들러:
    - 로그인 상태 체크 (비로그인 시 `/login`으로 navigate)
    - API 호출
    - `setProject(prev => prev ? { ...prev, likes: newCount } : null)` 로 하단 통계 동기화
    - `setLikeCount` 동기화
- **검증**: PlayPage에서 좋아요 클릭 후 버튼 숫자 = 하단 통계 숫자

### Task 6 — [#30] Follow 모델 + 팔로우 API (4분)
- **파일**: `backend/app/models/follow.py` (신규), `backend/app/routers/follows.py` (신규)
- **구현**:
  - `Follow` 모델: `follower_id`, `followee_id` (UniqueConstraint)
  - `POST /users/{user_id}/follow` — 팔로우
  - `DELETE /users/{user_id}/follow` — 언팔로우
  - `GET /users/me/following` — 내 팔로잉 목록 (닉네임, 아바타, 작품 수 포함)
  - `backend/app/main.py`에 라우터 등록
- **검증**: API 엔드포인트 동작 확인

### Task 7 — [#30] 프론트엔드 팔로우 API + DashboardPage 탭 전환 (5분)
- **파일**: `frontend/src/api/follows.ts` (신규), `frontend/src/pages/DashboardPage/DashboardPage.tsx`
- **구현**:
  - `getFollowing()` — 내 팔로잉 목록
  - `followUser(userId)` / `unfollowUser(userId)`
  - DashboardPage: `tab` 상태 (`'projects' | 'liked' | 'following'`)
  - 사이드바 링크에 `onClick={() => setTab('liked')}` / `onClick={() => setTab('following')}` 연결
  - 메인 영역: 탭별 컨텐츠 렌더링
    - `projects`: 기존 내 프로젝트 그리드
    - `liked`: 좋아요한 프로젝트 그리드
    - `following`: 팔로우한 유저 카드 목록
- **검증**: 사이드바 링크 클릭 시 탭 전환 + 컨텐츠 표시
